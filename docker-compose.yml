version: "3.9"

services:
  whisper:
    build: .
    container_name: whisper-web
    ports:
      - "8000:8000"
    volumes:
      - ./data:/data
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [gpu]
    restart: unless-stopped

